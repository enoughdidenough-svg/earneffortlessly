'use client'
import {useEffect,useState} from 'react'
export default function AdminModeToggle(){const [advanced,setAdvanced]=useState(false);useEffect(()=>{const value=localStorage.getItem('admin_mode')==='advanced';setAdvanced(value);document.documentElement.dataset.adminMode=value?'advanced':'simple'},[]);function toggle(){const next=!advanced;setAdvanced(next);localStorage.setItem('admin_mode',next?'advanced':'simple');document.documentElement.dataset.adminMode=next?'advanced':'simple'}return <button className="button" type="button" onClick={toggle} aria-pressed={advanced}>{advanced?'Advanced admin':'Simple admin'} · switch</button>}
