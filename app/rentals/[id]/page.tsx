import Link from 'next/link';
import { createServerSupabase } from '@/lib/supabase';
export default async function RentalDetail({params}:{params:Promise<{id:string}>}){const{id}=await params;const s=await createServerSupabase();const{data:r}=await s.from('rental_listings').select('*').eq('id',id).single();if(!r)return <main className='container'><div className='error'>Rental listing not found.</div></main>;return <main className='container'><p><Link href='/rentals'>← Rentals</Link></p><div className='card'><span className='badge'>{r.platform}</span><h1>{r.account_handle}</h1><p>{r.subscriber_count||0} subscribers</p><div className='price'>{r.price}</div><p className='muted'>One full day. Access uses {r.access_method||'official platform integration when connected'}.</p><form action='/api/rentals' method='post'><input type='hidden' name='listingId' value={id}/><button className='primary'>Request 24-hour rental</button></form></div></main>}
