'use client';import{useState}from'react';
export default function Upgrades(){const[instruction,setInstruction]=useState('');const[files,setFiles]=useState<FileList|null>(null);const[result,setResult]=useState('');const[busy,setBusy]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);const form=new FormData();form.set('instruction',instruction);if(files)Array.from(files).forEach(f=>form.append('files',f));const r=await fetch('/api/admin/upgrades',{method:'POST',body:form});const j=await r.json();setResult(JSON.stringify(j,null,2));setBusy(false)}return <main className='container'><div className='card wide'><div className='eyebrow'>UPGRADE CENTER</div><h1>Preview-first upgrades</h1><p className='muted'>Text, screenshots, documents, code and other inputs become a stored upgrade request. Production release remains an explicit admin decision.</p><form onSubmit={submit}><textarea className='input' rows={6} value={instruction} onChange={e=>setInstruction(e.target.value)} placeholder='Describe the change…' required/><input type='file' multiple onChange={e=>setFiles(e.target.files)}/><button className='primary' disabled={busy}>{busy?'Saving…':'Create upgrade preview request'}</button></form>{result&&<pre style={{whiteSpace:'pre-wrap'}}>{result}</pre>}</div></main>}
