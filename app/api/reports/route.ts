import{NextResponse}from'next/server';import{createServerSupabase}from'@/lib/supabase';
export async function POST(req:Request){const s=await createServerSupabase();const{data:{user}}=await s.auth.getUser();if(!user)return NextResponse.json({error:'Sign in required'},{status:401});const b=await req.json().catch(()=>({}));const targetType=String(b.targetType||'');const targetId=b.targetId?String(b.targetId):null;const reason=String(b.reason||'').trim();const description=String(b.description||'').trim();if(!targetType||!reason)return NextResponse.json({error:'Target and reason are required'},{status:400});const{data,error}=await s.from('user_reports').insert({reporter_id:user.id,target_type:targetType,target_id:targetId,reason,description}).select('id,status').single();if(error)return NextResponse.json({error:error.message},{status:400});return NextResponse.json({ok:true,report:data});}
export async function GET(){const s=await createServerSupabase();const{data:{user}}=await s.auth.getUser();if(!user)return NextResponse.json({error:'Sign in required'},{status:401});const{data,error}=await s.from('user_reports').select('id,target_type,target_id,reason,description,status,created_at,resolved_at').eq('reporter_id',user.id).order('created_at',{ascending:false});return NextResponse.json({data:data||[],error:error?.message});}
