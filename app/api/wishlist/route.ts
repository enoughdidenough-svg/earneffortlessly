import {NextResponse} from 'next/server';import{createServerSupabase}from'@/lib/supabase';
async function auth(){const s=await createServerSupabase();const{data:{user}}=await s.auth.getUser();return {s,user}}
export async function POST(req:Request){const{s,user}=await auth();if(!user)return NextResponse.redirect(new URL('/login',req.url));const ct=req.headers.get('content-type')||'';const b=ct.includes('json')?await req.json().catch(()=>({})):Object.fromEntries((await req.formData()).entries());const productId=String(b.productId||'');if(!productId)return NextResponse.json({error:'Product required'},{status:400});const{error}=await s.from('wishlists').upsert({user_id:user.id,product_id:productId},{onConflict:'user_id,product_id'});if(error)return NextResponse.json({error:error.message},{status:400});return NextResponse.redirect(new URL('/buyer/wishlist',req.url));}
export async function DELETE(req:Request){const{s,user}=await auth();if(!user)return NextResponse.json({error:'Sign in required'},{status:401});const b=await req.json().catch(()=>({}));const{error}=await s.from('wishlists').delete().eq('user_id',user.id).eq('product_id',b.productId);return NextResponse.json({ok:!error,error:error?.message});}
