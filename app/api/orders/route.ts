import{createServerSupabase}from'@/lib/supabase';import{NextResponse}from'next/server';
export async function POST(req:Request){const s=await createServerSupabase();const{data:{user}}=await s.auth.getUser();if(!user)return NextResponse.redirect(new URL('/login',req.url));const form=await req.formData();const product_id=String(form.get('product_id')||'');const{data:p}=await s.from('products').select('*').eq('id',product_id).single();if(!p)return NextResponse.json({error:'Product not found'},{status:404});const{error}=await s.from('orders').insert({buyer_id:user.id,product_id:p.id,seller_id:p.seller_id,amount:p.price,currency:p.currency,payment_method:'manual',status:'pending'});if(error)return NextResponse.json({error:error.message},{status:400});return NextResponse.redirect(new URL('/buyer',req.url));}
