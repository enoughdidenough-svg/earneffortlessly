import{createServerSupabase}from'@/lib/supabase';import{redirect}from'next/navigation';
export default async function Checkout({searchParams}:{searchParams:Promise<{id?:string}>}){const id=(await searchParams).id;const s=await createServerSupabase();const{data:{user}}=await s.auth.getUser();if(!user)redirect('/login');const{data:p}=await s.from('products').select('*').eq('id',id||'').single();if(!p)return <main className="container"><div className="error">Product not found.</div></main>;return <main className="container"><div className="card"><h1>Checkout</h1><p>{p.title}</p><div className="price">{p.currency} {p.price}</div><form action="/api/orders" method="post"><input type="hidden" name="product_id" value={p.id}/><button className="btn">Create order</button></form><p className="muted">Payment verification can be connected by the admin.</p></div></main>}
