import Link from 'next/link';import{createServerSupabase}from'@/lib/supabase';
export default async function Product({params}:{params:Promise<{id:string}>}){const{id}=await params;const s=await createServerSupabase();const{data:p}=await s.from('products').select('*').eq('id',id).single();if(!p)return <main className='container'><div className='error'>Listing not found.</div></main>;const{data:seller}=await s.from('profiles').select('username,display_name,reputation_score').eq('id',p.seller_id).single();const{data:reviews}=await s.from('marketplace_reviews').select('rating,body,created_at').eq('product_id',id).eq('status','published').order('created_at',{ascending:false}).limit(20);return <main className='container'><div className='card wide'><span className='badge'>{p.category}{p.subcategory?` · ${p.subcategory}`:''}</span><h1>{p.title}</h1><p className='muted'>{p.description}</p><div className='grid2'><div><p><b>Seller:</b> {seller?.username?<Link href={`/seller/${seller.username}`}>@{seller.username}</Link>:'—'}</p><p><b>Quality:</b> {p.quality_label||'Not rated yet'}</p><p><b>Rating:</b> {Number(p.rating_avg||0).toFixed(1)} ({p.rating_count||0})</p><p><b>Contents:</b> {p.contents_count||1}</p><p><b>License:</b> {p.license_type||'Not specified'}</p><p><b>Commercial use:</b> {p.commercial_use?'Yes':'No/unspecified'}</p><p><b>Formats:</b> {Array.isArray(p.file_types)?p.file_types.join(', '):p.file_types||'See listing'}</p><p><b>Compatibility:</b> {p.compatible_with||'Not specified'}</p></div><div><div className='price'>{p.currency} {p.price}</div><p className='muted'>{p.delivery_info||'Digital delivery after verified payment.'}</p><div className='row'><Link className='primary' href={`/checkout?id=${p.id}`}>Buy now</Link><form action='/api/cart' method='post'><input type='hidden' name='productId' value={p.id}/><button className='btn' type='submit'>Add to cart</button></form><form action='/api/wishlist' method='post'><input type='hidden' name='productId' value={p.id}/><button className='btn' type='submit'>Wishlist</button></form></div></div></div></div><section className='card wide'><h2>Reviews</h2>{reviews?.length?(reviews.map((r:any)=><article className='card soft' key={r.created_at+r.rating}><b>{'★'.repeat(r.rating)}</b><p>{r.body}</p></article>)):<p className='muted'>No reviews yet.</p>}</section></main>}
